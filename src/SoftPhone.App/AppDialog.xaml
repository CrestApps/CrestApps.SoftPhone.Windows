<Window x:Class="SoftPhone.App.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Soft Phone" Width="440" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" FontSize="13">
    <!-- The same card as the incoming-call popup: white, rounded, soft shadow. Drag it by the card. -->
    <Border Background="White" CornerRadius="10" Margin="12" BorderBrush="{StaticResource Line}" BorderThickness="1"
            MouseLeftButtonDown="Card_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.28" Color="#334155" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,20,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border x:Name="IconCircle" Width="40" Height="40" CornerRadius="20" VerticalAlignment="Top">
                    <TextBlock x:Name="IconGlyph" FontFamily="Segoe MDL2 Assets" FontSize="18"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="14,1,0,0">
                    <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource Ink}" TextWrapping="Wrap" />
                    <TextBlock x:Name="MessageText" Margin="0,6,0,0" Foreground="{StaticResource Muted}"
                               TextWrapping="Wrap" LineHeight="19" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Margin="0,20,0,0" Padding="20,12" Background="{StaticResource Panel}"
                    BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Style="{StaticResource SecondaryButton}" Content="Cancel"
                            Click="Cancel_Click" />
                    <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}" Content="OK"
                            Margin="8,0,0,0" Click="Confirm_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
